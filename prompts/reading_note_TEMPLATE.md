# reading_note_TEMPLATE.md — 单篇生产 prompt 骨架

> **状态：PLACEHOLDER**  
> Phase 0 发酵完成后，按 `paper_type` 复制为本文件并另存为 `reading_note_{type}.md`（如 `reading_note_rct.md`）。  
> **下列小节标题均为 TBD，不得直接用于生产。**

---

## 组装方式

生产时按顺序拼接：

1. `prompts/_shell.md` 全文（定稿版）
2. 本文件「类型任务 + 输出模板」部分（去掉 PLACEHOLDER 说明）

---

## 类型元数据（发酵时填写）

| 字段 | 值 |
|------|-----|
| `paper_type` | `TBD`（如 `rct` / `cohort_case` / `review` / `basic` / `unknown`） |
| `schema_version` | `TBD`（如 `v1`） |
| `适用文献` | TBD — 从样本归纳的一句话描述 |

---

## 类型任务（TBD）

<!-- 发酵后替换：本类型导读应侧重什么、应忽略什么、与其它类型的边界 -->

（待发酵：描述本类型文献的阅读重点与常见误区。）

---

## 输出模板（TBD）

<!-- 发酵后替换：以下为真实三级标题列表，模型必须逐项填写 -->

### [小节 1 — 标题待定]

（待发酵：本节应写什么、多长、是否 bullets。）

### [小节 2 — 标题待定]

（待发酵。）

### [小节 3 — 标题待定]

（待发酵。）

<!-- 发酵时增删小节至覆盖该类型的必读信息；数量与命名仅由样本决定 -->

---

## 路由提示（可选，发酵时决定是否保留）

若 L1 元信息已提供，可先根据标题与摘要判断是否符合本 `paper_type`；若明显不符，在首段用一行注明「类型可能不匹配：…」，仍按本模板输出。
