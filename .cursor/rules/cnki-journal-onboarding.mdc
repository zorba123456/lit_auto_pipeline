---
description: 新增 CNKI 期刊时的完整接入规范
globs: aes-feeds/cnki_*.json,aes-feeds/cnki_downloader.py,feed_reader.html
alwaysApply: true
---

新增 CNKI 期刊前，**立即阅读并完整执行** `AGENTS.md` 中「新增 CNKI 期刊」章节的 7 个步骤。

核心要点：必须深度爬取（`--mode web`）、必须先清去重（`--reset-journal`）、Inoreader 用 `_cleaned` URL。
