---
description: AES 文献工作台设计 — 主文档维护与新对话接续
globs:
alwaysApply: true
---

# AES 文献工作台 · 主文档规则

## 唯一主文档

`docs/aes_workbench_design.md` — 产品/架构**唯一 living document**。设计讨论不要拆到其他模块 md（C10）。

## 新对话接续（无跨 chat ID，靠文件对接）

1. **禁止 bootstrap 阶段 Read 全文**（~1900 行会占满上下文）。`@` 整文件常已预载 **40%+**，勿再通读。
2. **首轮只读**：文件 **第 1–65 行**（修订记录 + C 表）+ **§15 待决**；正文 § 等用户话题明确后再 `Read` 该段或 `Grep` 定位。
3. **不要**依赖旧对话 recap；以文档正文为准（C11）。不必预先指定 §，除非用户窄任务。
4. 用户 `@` 后上下文已高 → 用 §21.3 模板 E，本轮不再 Read 除头+§15 外的章节。

## 何时写回文档

| 情况 | 动作 |
|------|------|
| 产品/架构**新结论**、推翻旧决策、关闭待决 | 更新正文 +/或 C 表 +/或 §15；大变更升修订记录版本 |
| 纯问答、澄清、无新决策 | 可只回复，不改文件 |
| 用户未要求 commit | **不要** git commit |

## 相关但独立

- CNKI 源接入 → `AGENTS.md`
- 豆包 RPA / batch_worker / 浏览器分配 → `AES_PIPELINE_HANDOFF.md`（豆包=chip 链；导读=Gemini Web）
